<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <title>URL Shortner</title>
    
</head>
<body>
    <div class="container">
        <h1>URL Shrinker</h1>
        <form action="/shortUrls" method="POST" class="form-inline my-4">
            <label for="fullUrl" class="sr-only">URL</label>
            <input required placeholder="URL" class="form-control col mr-2" type="url" name="fullUrl" id="fullUrl">
            <br>
            <button type="submit" class="btn btn-success" >Shrink</button>
        </form>
        <table class="table table-striped table-responsive">
            <thead>
                <tr>
                    <th>Full URL</th>
                    <th>Short URL</th>
                    <th>Clicks</th>
                </tr>
            </thead>
            <tbody>
                <% shortUrls.forEach(shortUrl =>{ %> 
                    <tr>
                        <td><a href="<%= shortUrl.full %>"><%= shortUrl.full %> </a></td>
                        <td><a href="<%= shortUrl.short %> "><%= shortUrl.short %></a></td>
                        <td><a href="<%= shortUrl.clicks %> "><%= shortUrl.clicks %></a></td>
                    </tr>
                    <% }) %> 
            </tbody>
        </table>
    </div>

    <div style="position:fixed;
    bottom:0;
    left:0;
    width:100%;
    height: 90px;
    background-color: #D0DAEE;
    text-align: center;
    ">
        <b><p style="padding-top: 25px;">Developed by Sai kumar Silaparasetti</p></b>
    </div>
</body>
</html>